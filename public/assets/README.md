# Logo assets

Place the two OrangeKite logo files here, using these exact filenames
(already referenced throughout the codebase):

- `orangekite-icon_svg.png` — the hawk mark only, used in the nav and footer.
- `orangekite-logo_svg.png` — the full lockup (hawk + wordmark), used for the
  Open Graph / social preview image.

Both should ideally have a transparent background. Until these files are
added, `next/image` will 404 for the icon and the OG preview image will be
missing.
